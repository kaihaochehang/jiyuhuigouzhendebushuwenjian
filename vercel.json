{
  "version": 2,
  "buildCommand": "npm install && npm run build || echo 'No build script'",
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/server.js"
    }
  ]
}
